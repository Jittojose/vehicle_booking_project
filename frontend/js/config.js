// frontend-bootstrap/js/config.js
// Single place to configure API base URL and optional auth.

export const API_BASE = 'http://localhost:8000/api';

// If you add authentication later, you can set a token here and
// have api.js attach it as an Authorization header.
export const AUTH_TOKEN = null; // e.g. 'Bearer <token>'
